import { GallerySlidingDirectiveDirective } from './gallery-sliding-directive.directive';

describe('GallerySlidingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GallerySlidingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
