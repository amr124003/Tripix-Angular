import { RippleButtonDirective } from './ripple-button.directive';

describe('RippleButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RippleButtonDirective();
    expect(directive).toBeTruthy();
  });
});
